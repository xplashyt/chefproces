/** @type {import('next').NextConfig} */
const nextConfig = {
  // Modo estricto: avisa en desarrollo de efectos mal escritos en el único
  // componente cliente del sitio (el panel de checkout).
  reactStrictMode: true,
};

export default nextConfig;
